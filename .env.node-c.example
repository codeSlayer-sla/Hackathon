# Copy to .env.node-c on Node C (the laptop running docker-compose.node-c.yml)
# and set this to Node A's real LAN IP (not localhost). See
# docs/multi-host-demo.md for how to find it.
ROUTER_HOST=192.168.1.23
